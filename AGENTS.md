# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

(Pinned to SDK 54 so the project runs in the App Store version of Expo Go. SDK 56 is not yet supported by the public Expo Go.)
